# Contributing to SPACE INDEX

ご協力ありがとうございます。本プロジェクトは日本の宇宙産業エコシステムへの参入支援を目的とした情報基盤で、コミュニティからの情報追加・修正を歓迎します。

## 貢献の方法

### Issue で報告する

以下のような情報があれば、Issue を立ててお知らせください：

- **誤った情報や古い情報**：価格・採択額の変動、リンク切れ、誤記など
- **新しい情報源**：信頼できる業界レポート、公式資料、データプラットフォーム等
- **新しい政府支援プログラム**：新規公募・採択結果・補助制度
- **追加してほしい機能・セクション**

### Pull Request で直接編集する

データの追加・修正は、`index.html` 内の該当する JavaScript 配列を編集してください。

#### 情報源の追加例

`resources` 配列に追記します：

```javascript
{
  name: "情報源の名称",
  org: "提供元組織",
  desc: "1〜2文の簡潔な説明。価格情報があれば含める。",
  url: "https://example.com",
  cost: "free",        // free / paid / freemium
  region: "JP",        // JP / GLOBAL
  cat: ["upstream"],   // 該当カテゴリ複数可
  info: ["business"],  // 情報種別複数可
  tags: ["タグ1", "タグ2", "タグ3"]
}
```

**フィールドの値**：

| フィールド | 取りうる値 |
|---|---|
| `cost` | `free`（無償）/ `paid`（有償）/ `freemium`（一部無償） |
| `region` | `JP`（日本中心）/ `GLOBAL`（国際） |
| `cat` | `upstream` / `midstream` / `downstream` / `frontier` / `support` / `cross` |
| `info` | `market` / `business` / `players` / `tech` / `policy` / `funding` / `data` / `event` / `career` / `media` |

#### 政府支援プログラムの追加例

`businessModels` 配列の該当エントリ内の `subsidies` 配列に追記します：

```javascript
{
  name: "宇宙戦略基金（経産省）",
  theme: "テーマ名（採択公募の正式名称）",
  amount: "総額○○億円／1件上限○○億円",
  type: "委託",        // 委託 / 補助 / 委託/補助 / その他
  note: "採択件数や応募条件の補足（任意）"
}
```

## レビューの観点

PR をマージする際は以下を確認します：

- **正確性**：公式情報源（JAXA・内閣府・経産省等）に基づくか
- **新鮮さ**：1年以内に確認できる情報か
- **中立性**：特定企業・サービスの宣伝にならないか
- **記述スタイル**：既存エントリと統一感があるか
- **動作確認**：ブラウザで `index.html` を開いて崩れがないか

## スタイルガイド

- **言語**：データ・UIテキストは日本語を基本（英語の対訳は補助的に）
- **句読点**：「、」「。」を使用、半角文字との間にスペースは入れない
- **数値**：日本語の慣用に従う（例：「数百億円」「総額950億円」）
- **URL**：公式ページを優先（SNS・ブログ等は補足として）
- **タグ**：3つ以内、簡潔・具体的に

## 連絡先

質問・提案は Issue でお気軽にどうぞ。

ありがとうございます。
